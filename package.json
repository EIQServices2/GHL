{
  "name": "ghl",
  "version": "0.1.0",
  "private": true,
  "description": "EIQ-GHL1 — GHL decommission: NextJS frontend clone of TDU rate-change pages, hosted on powerrateindex.org",
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "lint": "npm run lint --prefix web"
  }
}
